export default function Header() {
    return (
        <div className="navbar bg-primary text-neutral-content flex justify-center">
            <button className="btn btn-ghost text-xl">What's the weather like?</button>
        </div>
    );
}
